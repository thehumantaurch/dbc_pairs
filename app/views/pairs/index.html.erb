<p>
<div class="pair_title"><h1>Your *pairs*</h1></div>
</p>
<div class="pairs">
  <ol>
  <% @pairs.each do |p| %>
    <li><% p.each do |student| %>
      <p class='pair'><%= link_to "#{student.name}", (student_path(student)) %></p>
    <% end %></li>
  <% end %>
  </ol>
</div>
